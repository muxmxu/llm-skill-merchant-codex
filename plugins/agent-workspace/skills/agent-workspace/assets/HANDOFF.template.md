# HANDOFF — <topic>

Last updated: <YYYY-MM-DD>

## Read first

1. `STATUS.md` in this directory — current step.
2. `worklog.md` in this directory — what has happened and what was rejected.
3. <external protocol / contract / prior directory this work depends on>

## Minimum context

<Only what is NOT recoverable from the two files above: the protocol in play,
the target output location, constraints discovered but not written anywhere
else. Do not restate the worklog.>

## Waiting on the human

- <what is blocked on a ruling, and what the options are. Write "nothing" if
  nothing is.>

<!-- Pointers, not copies. Rewrite this file to the current moment at each
     handoff rather than appending to it. -->
